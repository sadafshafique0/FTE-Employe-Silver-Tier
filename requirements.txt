# AI Employee Silver Tier — Python Dependencies

# ── Bronze Tier (required) ─────────────────────────────────────────────────
# File system watcher
watchdog>=4.0.0

# ── Silver Tier (required) ─────────────────────────────────────────────────
# Gmail API — read inbox + send emails
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.0.0

# LinkedIn + WhatsApp Web automation
playwright>=1.40.0

# Install playwright browsers after pip install:
#   playwright install chromium
